// Generated by LiveScript 1.3.1
(function(){
  var http, connect, ecstatic, log, app, slice$ = [].slice;
  http = require('http');
  connect = require('connect');
  ecstatic = require('ecstatic');
  log = function(){
    var args;
    args = slice$.call(arguments);
    console.log.call(console, ([new Date()].concat(args)).join(" "));
  };
  app = connect();
  app.use(ecstatic({
    root: __dirname,
    defaultExt: 'html',
    handleError: false
  }));
  app.use(function(req, res){
    res.end('Hello, world!');
  });
  http.createServer(app).listen(9985, function(){
    log('Server started');
  });
}).call(this);
